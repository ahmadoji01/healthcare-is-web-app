
interface CashProps {
    total: number,
    confirmPayment: () => void,
}

const CashPayment = ({ total, confirmPayment }: CashProps) => {
    
    return (
        <></>
    );
}

export default CashPayment;